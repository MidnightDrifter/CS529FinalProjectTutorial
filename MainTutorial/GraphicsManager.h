#pragma once
/*
#include <glew.h>
#include <glfw3.h>
class GraphicsManager
{
public:

	enum class SHADER_TYPE
	{
		COLOR,
		TEXTURE,
		NUM
	};

	GraphicsManager();
	~GraphicsManager();

	GLint globalShaderIDs[(int)(SHADER_TYPE::NUM)];


private:

};
*/
